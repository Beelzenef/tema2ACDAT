<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tema2acdat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AgendaActivity" />
        <activity android:name=".DiasLectivosActivity" />
        <activity android:name=".NavegadorActivity" />
        <activity android:name=".VisorImgsActivity" />
        <activity android:name=".DivisasActivity" />
        <activity android:name=".UploadFileActivity" />
        <activity android:name=".AlarmasActivity"></activity>
    </application>

</manifest>